/**
 * Utility functions for Nosskey SDK
 * @packageDocumentation
 */

import { NosskeyConfig } from './types'

/**
 * Creates a new Nosskey instance
 * @param config - Configuration options
 * @returns A new Nosskey instance
 */
export function createNosskey(config: NosskeyConfig) {
  // TODO: Implement
  return {}
} 